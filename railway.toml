[build]
builder = "nixpacks"
buildCommand = "cd backend && npm install && npx prisma generate && npx prisma migrate deploy && npx tsc -p tsconfig.build.json"

[deploy]
startCommand = "cd backend && node dist/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
NODE_ENV = "production" 